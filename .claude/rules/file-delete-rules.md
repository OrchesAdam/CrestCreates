### 删除命令
- 你不要直接执行文件删除操作的命令
- 若需清理文件或文件夹，只能将对象移动到工作目录的根目录下的`./99_RecycleBin`,如果工作目录下没有，则新建这个目录，并把清理文件或文件夹移动过去。
- 移入`./99_RecycleBin` 后，默认不得再对该对象执行重命名、压缩、拆分、合并或二次整理;仅允许保留原层级暂存。
- 只有用户在图形界面中手动删除，才视为进入系统回收站的受控删除。