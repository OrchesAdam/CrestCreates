/// <summary>
/// Assembly marker for the Agent.Runtime aggregate project.
/// This project serves as a future composition root for Agent runtime implementations.
/// Currently empty — reserved for when a unified Agent runtime facade is needed
/// (e.g., a single DI registration method composing Memory + Authoring + ControlPlane).
/// </summary>
namespace CrestCreates.Agent.Runtime;

public sealed class AssemblyMarker
{
}
